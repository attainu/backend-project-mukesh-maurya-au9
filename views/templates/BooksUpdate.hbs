<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/css/bootstrap.min.css">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/js/bootstrap.min.js"></script>
    <title>Document</title>
    {{!-- {{>main}} --}}
    <style>
    /* Set height of the grid so .sidenav can be 100% (adjust as needed) */
    .row.content {height: 100vh}
    /* Set gray background color and 100% height */
    .sidenav {
      background-color: #f1f1f1;
      height: 100%;
    }
    .admin{
      position: fixed;
    }
    .other{
      margin-left:350px;
      margin-right: 15px;
    }
    /* On small screens, set height to 'auto' for the grid */
    @media screen and (max-width: 767px) {
      .row.content {height: auto;} 
    }
  </style>
</head>
<body>
{{>navbar}}
<div class="container-fluid admin">
  <div class="row content">
    <div class="col-sm-3 sidenav hidden-xs admin">
      <h2>Admin Panel</h2>
      <ul class="nav nav-pills nav-stacked">
        <li><a href="/admin/dashboard">Dashboard</a></li>
        <li><a href="/admin/allUsers">Users Management</a></li>
        <li ><a href="/admin/orderlist">Order List</a></li>
        <li class="active"><a href="/admin/bookupdate">Books Update</a></li>
      </ul><br>
    </div>
    <br>
    
    <div class="col-sm-9 container other">
      <div class="well">
        <h4>Welcome </h4> {{!-- Display User Name--}}
        <p>Some text..</p>
      </div>
      <div class="table-reponsive container">
            <center> <h2>Hi Admin{{usersName.firstName}} </h2></center>
          <div class="col-md-10">
            <table class="table ">
                <thead>
                  <tr>
                    <th>Title</th>
                    <th>Price</th>
                    <th>IsActive</th>
                    <th>Category</th>
                  </tr>
                </thead>
                <tbody>
                    {{# each allBooks}}
                    <tr>
                        <td>{{this.title}}</td>
                        <td>{{this.price}}</td>
                        <td>{{this.isActive}}</td>
                        <td>{{this.category}}</td>
                    </tr>
                    {{/ each}}
                </tbody>
            </table>
          </div>
        </div>   
      </div>
    </div>
  </div>
</div>
</body>
</html>